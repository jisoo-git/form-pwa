# 인코딩플러스 폼 PWA 사용법

1. **관리자 접근** — 홈 우상단 "관리자" 클릭 → 아무 비밀번호나 입력해 로그인
2. **폼 만들기** — 대시보드에서 "새 폼 만들기" → 템플릿 선택 후 편집
3. **폼 활성화** — 폼 카드의 `비활성` 버튼 클릭해 `활성`으로 변경 (필수, 안 하면 폼 접근 불가)
4. **홈 연결** — "홈 연결 설정"에서 활성화한 폼을 각 과정 버튼에 연결
5. **응답 확인** — "응답 보기"로 제출 내역 열람, "링크 복사"로 폼 URL 직접 공유
